import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
      height: 40,
      margin: 12,
      borderWidth: 1,
      padding: 10,
   
      },
      title:{
        flex:1
      }

})